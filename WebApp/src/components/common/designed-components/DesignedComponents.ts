import { CardHeader, styled } from "@mui/material";

export const CustomDesignComponents = {
  CustomCardHeader: styled(CardHeader)({
    fontSize: 18,
    color: "#ededed",
    backgroundColor: "#828282",
  }),
};

export const CustomTab = {
    
}
